{% extends sonata_block.templates.block_base %}

{% block block %}
    <div class="box box-primary">
        <div class="box-header">
            <h3 class="box-title">{{ 'admin.workorder.block_title_list' | trans }}</h3>
        </div>
        <div class="box-body table-responsive no-padding">
            <table class="table table-bordered table-striped table-hover sonata-ba-list">
                <thead>
                <tr class="sonata-ba-list-field-header">
                    <th>{{ 'enum.audit_status.pending' | trans }}</th>
                    <th>{{ 'enum.audit_status.pending' | trans }}</th>
                    {#                    <td>#}
                    {#                        <div class="btn-group">#}
                    {#                            <a href="#" class="btn btn-link btn-flat"><i class="fa fa-pause-circle"></i> {{ pending_audits }}</a>#}
                    {#                        </div>#}
                    {#                    </td>#}
                </tr>
                </thead>
                <tbody>
                <tr>
                    {% for work_order in not_finished_work_orders %}
                        <td>{{ work_order.id }}</td>
                        <td>{{ work_order.id }}</td>
                    {% endfor %}
                </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
